import React, { useState } from 'react';
import {
  StyleSheet,
  Text,
  View,
  TextInput,
  TouchableOpacity,
  KeyboardAvoidingView,
  Platform,
  SafeAreaView,
  Alert,
  SafeAreaViewBase,
} from 'react-native';
const HomePage: React.FC = () => {
  return <SafeAreaViewBase></SafeAreaViewBase>;
};

export default HomePage;
